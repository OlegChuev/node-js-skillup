{
  "development": {
    "username": "$POSTGRES_USER",
    "password": "$POSTGRES_PASSWORD",
    "database": "$POSTGRES_NAME",
    "host": "$POSTGRES_HOST",
    "dialect": "postgres"
  },
  "test": {
    "username": "$POSTGRES_USER",
    "password": "$POSTGRES_PASSWORD",
    "database": "$POSTGRES_NAME",
    "host": "$POSTGRES_HOST",
    "dialect": "postgres"
  }
}
